import mixins from "./mixins";

const theme = {
    bp: {
       xs: "max-width: 600px",
       sm: "max-width: 960px",
       md: "max-width: 1280px",
       lg: "max-width: 1920px"
    },
    mixins
}

export default theme;